# The desktop app (python -m football_analysis.app). Everything the analysis
# needs, plus Qt for the window, the camera list and the camera itself.
# On a Mac with Apple silicon the torch wheel pip picks already runs on the GPU (MPS).
-r requirements-detect.txt
PySide6>=6.6,<7.0
